"""the daily session spine.

three phases: mood capture (one word, one number) -> pattern check (Dark Days or a
normal session) -> save and reflect (logs, Echo Distance, weekly/monthly milestones).

reads/writes data/echo_log.csv, data/learning_log.csv
"""

# lands with issue #4
