# Optional. A local, offline transformer for a sharper emotion read - free, no
# API, nothing ever leaves your machine. EchoSelf works fully without it (the
# lexicon in core/emotion is the floor); install only if you want the deeper NLP:
#   pip install -r requirements-nlp.txt
# heads up: this pulls in PyTorch - a large (~1-2 GB) download - and the emotion
# model (~300 MB) downloads once on first use. set ECHOSELF_NLP_MODEL to swap it.
transformers>=4.40
torch>=2.0
